spring.application.name=internship_ai_backend

spring.datasource.url=jdbc:mysql://localhost:3306/internship_ai
spring.datasource.username=root
spring.datasource.password=Akhil@2006
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

server.port=8089

automation.internships.endpoint=http://localhost:5678/webhook/internship-recommendations

# ─── File Upload ────────────────────────────────────────
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# ─── CORS ───────────────────────────────────────────────
app.cors.allowed-origins=*

# Google OAuth
app.oauth.google.client-id=489484268154-mllmp32m1cpk8db03sfcq8bstl01ogpt.apps.googleusercontent.com
app.oauth.google.client-secret=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
app.oauth.google.redirect-uri=${GOOGLE_REDIRECT_URI:http://localhost:8089/api/auth/google/callback}
app.oauth.google.scope=email profile

# Frontend URL used after OAuth callback
# Default includes /frontend because this workspace serves static pages from that path.
app.frontend.base-url=${FRONTEND_BASE_URL:http://localhost:5500/frontend}